{
  "name": "@packages/domain",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"console.log('lint @packages/domain')\"",
    "test": "rm -rf .tmp-test && tsc -p tsconfig.test.json && node --test .tmp-test/identity/__tests__/identity.test.js",
    "build": "node -e \"console.log('build @packages/domain')\""
  }
}
