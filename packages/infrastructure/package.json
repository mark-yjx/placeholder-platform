{
  "name": "@packages/infrastructure",
  "private": true,
  "version": "0.0.0",
  "dependencies": {
    "@packages/application": "*",
    "@packages/contracts": "*",
    "@packages/domain": "*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"console.log('lint @packages/infrastructure')\"",
    "test": "rm -rf .tmp-test && tsc -p tsconfig.test.json && node --test .tmp-test/postgres/submission/__tests__/*.test.js",
    "build": "node -e \"console.log('build @packages/infrastructure')\""
  }
}
