{
  "name": "@placeholder/application",
  "private": true,
  "version": "0.0.0",
  "dependencies": {
    "@placeholder/contracts": "*",
    "@placeholder/domain": "*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"console.log('lint @placeholder/application')\"",
    "test": "rm -rf .tmp-test && tsc -p tsconfig.test.json && node --test .tmp-test/**/*.test.js",
    "build": "node -e \"console.log('build @placeholder/application')\""
  }
}
